name: POC - Build & Artifact

on:
  push:
    branches: [main, feature/add-artifact]
  workflow_dispatch:
    inputs:
      custom_message:
        description: 'Mensaje personalizado para el artefacto'
        required: false
        default: 'Ejecutado manualmente'

permissions:
  contents: read

jobs:
  build:
    name: Build Artifact
    runs-on: ubuntu-latest

    env:
      POC_MESSAGE: ${{ inputs.custom_message || secrets.POC_MESSAGE || 'Hola desde GitHub Actions!' }}
      OUT_DIR: dist

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Debug - Mostrar variables de entorno
        shell: bash
        run: |
          echo "=================================================="
          echo "Variables de GitHub Actions"
          echo "=================================================="
          echo "GITHUB_SHA: $GITHUB_SHA"
          echo "GITHUB_REF: $GITHUB_REF"
          echo "GITHUB_RUN_NUMBER: $GITHUB_RUN_NUMBER"
          echo "GITHUB_ACTOR: $GITHUB_ACTOR"
          echo "GITHUB_REPOSITORY: $GITHUB_REPOSITORY"
          echo "--------------------------------------------------"
          echo "Variables personalizadas"
          echo "--------------------------------------------------"
          echo "POC_MESSAGE: $POC_MESSAGE"
          echo "OUT_DIR: $OUT_DIR"
          echo "=================================================="

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Generar artefacto
        run: python scripts/generate.py

      - name: Subir artefacto
        uses: actions/upload-artifact@v4
        with:
          name: build-artifact-${{ github.run_number }}
          path: dist/*
          if-no-files-found: error
          retention-days: 7

      - name: Notificar a Teams
        if: always()
        env:
          TEAMS_WEBHOOK_URL: ${{ secrets.TEAMS_WEBHOOK_URL }}
        run: python scripts/notify_teams.py
